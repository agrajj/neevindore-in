import { ProdImageDirective } from './prod-image.directive';

describe('ProdImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ProdImageDirective();
    expect(directive).toBeTruthy();
  });
});
